A library that makes LOOP simple again.

examples:

```
(@:loop
  (@:exit-when <condition>)
  <code>)
```

```
(@:loop
  <code>
  (@:exit-when <condition>))
```

```
(@:loop
  <code>
  (@:exit-when <condition>)
  <code>)
```





